# Security Policy

## 脆弱性の報告

脆弱性を見つけた場合は **公開の Issue には書かず**、GitHub の
[Private vulnerability reporting](https://github.com/TaikiKawa/taskdeck/security/advisories/new)
から報告してください。内容はメンテナだけが閲覧できます。

報告には次を含めてもらえると助かります。

- 影響を受けるバージョン（Release のタグ、またはコミット）
- 再現手順、またはコンセプトコード
- 想定される影響（ローカルの何にアクセスできるか）

原則として 7 日以内に一次返信します。修正版の公開まで詳細は非公開にしてください。

## 対象バージョン

最新の Release のみ修正対象です。

## 前提となる脅威モデル

taskdeck は **利用者の PC の中だけで動く**ツールで、サーバーは `127.0.0.1` にしか bind しません。

- ネットワーク越しの攻撃は対象外です（ローカルにしか露出しません）
- 一方で、ローカルで動く Web サーバーと、タスクを元に `claude` CLI を起動する機能があるため、
  **同じ PC 上のブラウザで開いた悪意あるページからの操作**（CSRF / DNS rebinding）や、
  **タスク本文経由のプロンプトインジェクション**は正当な報告対象です
- 「全自動（`--dangerously-skip-permissions`）」モードは信頼できるタスクにだけ使う前提です
