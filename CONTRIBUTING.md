# Contributing

小さなツールなので、まずは Issue で相談してもらえると助かります。

## Issue

- バグ報告・機能要望はテンプレートに沿って書いてください
- **脆弱性は Issue に書かないでください**。[SECURITY.md](SECURITY.md) の手順で非公開に報告してください
- 質問や使い方の相談も Issue で構いません

## Pull Request

- 1 PR = 1 テーマにしてください。大きな変更は先に Issue で方向性を合わせます
- 依存パッケージの追加は理由を PR に書いてください（完全ローカル・最小依存の方針です）
- 外部ネットワークへ通信するコード、`claude` CLI の起動引数を変えるコード、
  Web サーバーのルーティングを変えるコードは、セキュリティ観点でレビューが厚くなります
- コードスタイルは既存ファイルに合わせてください（ESM、2 スペース、日本語コメント可）

## 開発環境

```bash
npm install
npm run ui          # http://localhost:4747
./macos/build.sh    # macOS アプリ（開発版）
```

CI では `npm ci` と各ファイルの構文チェック、サーバー起動のスモークテストを行います。

## ライセンス

コントリビュートしたコードは [MIT License](LICENSE) のもとで公開されることに同意したものとみなします。
