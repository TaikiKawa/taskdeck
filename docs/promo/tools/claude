#!/bin/sh
# Demo stand-in for the real `claude` CLI (used only when recording the promo).
# Point TASKDECK_CLAUDE at this file so dispatch runs fake-claude.mjs instead.
exec node "$(dirname "$0")/fake-claude.mjs" "$@"
